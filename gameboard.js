const gameBoard = (rows, cols)=>{
    
    const board = (rows = 10, cols = 10) => {
        const boardArray = []
        for(let i = 0; i<rows; i++){
            for(let j=0; j< cols; j++){
                boardArray.push[i][j]
            }
            return boardArray

        }
    }
}